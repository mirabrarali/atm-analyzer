export const metadata = {
  title: "ATM Analyzer Pro — Understanding JRN files",
  description:
    "Plain-English guide to ATM journal files and how we read them — plus a live extractor you can try.",
};

export default function JrnFormatLayout({ children }) {
  return children;
}
